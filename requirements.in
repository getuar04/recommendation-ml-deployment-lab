fastapi>=0.115
uvicorn>=0.30
SQLAlchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.2
python-dotenv>=1.0
pydantic>=2.8
# RS256 JWT/JWKS verification (app.core.jwt_auth, GET /auth/me -- Task: first isolated JWT
# authentication vertical slice). [crypto] pulls in `cryptography` for RSA signature
# verification -- never hand-rolled here, per that module's own docstring.
PyJWT[crypto]>=2.8
pandas>=2.2
numpy>=2.0
scikit-learn>=1.5
joblib>=1.4
pytest>=8.0
httpx>=0.27
# Gradient-boosting candidate algorithms for app.ml.algorithm_registry (multi-model VIDEO
# training comparison). catboost transitively pulls in matplotlib/plotly/graphviz (its own
# unused-here eval-visualization tooling) -- a real, accepted dependency-weight tradeoff, not
# an oversight; see app/ml/algorithm_registry.py.
xgboost>=2.1
lightgbm>=4.5
catboost>=1.2
